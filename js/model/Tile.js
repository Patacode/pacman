/**
 * Represents a fixed tile component like a wall or a dot.
 */
class Tile extends Component{

    /**
     * To be created, a Tile just need an id.
     *
     * @param id {string} unique tile's id
     */
    constructor(id) {
		super(id);
    }
}
